import { Placeholder } from "@/components/dashboard/placeholder";

export default function AuditPage() {
  return <Placeholder title="Audit" description="The append-only record of every action the engine took." />;
}
