import { Placeholder } from "@/components/dashboard/placeholder";

export default function OverviewPage() {
  return <Placeholder title="Overview" description="Sync health at a glance — metrics, lag, and the live event feed." />;
}
