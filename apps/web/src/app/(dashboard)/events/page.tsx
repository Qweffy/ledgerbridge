import { Placeholder } from "@/components/dashboard/placeholder";

export default function EventsPage() {
  return <Placeholder title="Events" description="The sync outbox — every event, its status, attempts, and retries." />;
}
