import { Placeholder } from "@/components/dashboard/placeholder";

export default function DemoPage() {
  return <Placeholder title="Demo" description="Drive the engine live — create an invoice, force a conflict, run the reconciler." />;
}
